// const express = require("express");
// const passport = require("passport")
// const users = express();

// users.post('/user',passport.Authenticator( 'signup',{session: false}), async (req, res) => {

//     return res.json({ status: true, user : req.user});
//   });

//   module.exports = users;