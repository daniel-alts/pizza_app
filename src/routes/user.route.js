const user = require('express').Router();
const userModel = require('../models/user.model');

user.post('/', (req, res) => {

}).get('/', (req, res) => {

})

user.get('/:_id', (req, res) => {

}).patch('/:_id', (req, res)=> {

}).delete('/:_id', (req, res) => {

})

module.exports = user;

